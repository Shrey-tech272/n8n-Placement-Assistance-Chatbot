# n8n-Placement-Assistance-Chatbot
 Architected a multi-node agentic workflow on n8n: Telegram webhook → intent classifier (GPT-4o) → conditional routing across FAQ retrieval, lead capture, and escalation nodes — all without human handoff for standard queries
